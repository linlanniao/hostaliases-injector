apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "sre-mutator.fullname" . }}-selfsigned-issuer
  labels:
  {{- include "sre-mutator.labels" . | nindent 4 }}
spec:
  selfSigned: {}